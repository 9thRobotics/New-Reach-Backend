{
  "name": "new-reach-backend",
  "version": "1.0.0",
  "description": "Backend for Reach Token",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mongoose": "^7.0.0"
  },
  "engines": {
    "node": ">=16.x"
  }
}
